export default {
    // baseUrl: 'http://localhost:3000/',
    // // socketUrl: process.env.NODE_ENV === 'production' ? 'https://capmagic.net:3443/socket' : 'http://localhost:3002/socket',
    // // socketUrl: socketUrl,
    // socketUrl: process.env.MIX_SOCKET_URL,
    // runLocalFunctionality: process.env.MIX_RUN_LOCAL_FUNCTIONALITY === "true" ? true : false,
    // catchErrorMessage: 'Oops! something went wrong. Please try again later.',
    status: {
      success: 'success',
      fail: 'fail'
    },
    message:{
        generalError: 'Oops! soemthing went wrong',
    }

  }
